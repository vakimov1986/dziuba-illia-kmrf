<UserControl x:Class="CurConvApp.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CurConvApp.ViewModels"
             xmlns:services="clr-namespace:CurConvApp.Services"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lex="http://wpflocalizeextension.codeplex.com">
    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="Реєстрація користувача"
                    FontSize="24"
                    FontWeight="Bold"
                    Margin="0 0 0 20"
                    HorizontalAlignment="Center"/>

            <TextBox materialDesign:HintAssist.Hint="Ім’я"
                  Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                  Margin="0 0 0 20"
                  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox materialDesign:HintAssist.Hint="Прізвище"
                  Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                  Margin="0 0 0 20"
                  Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox materialDesign:HintAssist.Hint="Електронна пошта"
                  Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                  Margin="0 0 0 20"
                  Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

            <PasswordBox materialDesign:HintAssist.Hint="Пароль"
                      Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                      Margin="0 0 0 20"
                       />

            <Button Content="Зареєструватися"
                 Style="{StaticResource MaterialDesignRaisedButton}"
                 Command="{Binding RegisterCommand}"
                 Margin="0 0 0 10"/>

            <Button Content="Назад до входу"
                 Style="{StaticResource MaterialDesignOutlinedButton}"
                 Command="{Binding GoToLoginCommand}" />
        </StackPanel>
    </Grid>
</UserControl>

