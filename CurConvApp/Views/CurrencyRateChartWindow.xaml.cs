using System.Windows;

namespace CurConvApp.Views
{
    public partial class CurrencyRateChartWindow : Window
    {
        public CurrencyRateChartWindow()
        {
            InitializeComponent();
        }

        // Тут додати параметри для передачі даних/валюти, якщо потрібно
    }
}
